#include <iostream>
#include <vector>
#include <map>

std::map<char, unsigned> count(const std::string& string);
std::vector<int> VectorSortMerge(std::vector<int>, std::vector<int>);
long zeros(long n);
std::vector<std::string> get_pins(std::string observed);
void zaMainC();
